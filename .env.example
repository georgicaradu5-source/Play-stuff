# X Agent Unified - Environment Configuration

# === AUTHENTICATION MODE ===
# Choose: "tweepy" (OAuth 1.0a) or "oauth2" (OAuth 2.0 PKCE)
X_AUTH_MODE=tweepy

# === TWEEPY MODE (OAuth 1.0a) ===
# Required for tweepy mode - get from https://developer.x.com/en/portal/dashboard
X_API_KEY=your_api_key_here
X_API_SECRET=your_api_secret_here
X_ACCESS_TOKEN=your_access_token_here
X_ACCESS_SECRET=your_access_secret_here

# === OAUTH 2.0 MODE ===
# Required for oauth2 mode
X_CLIENT_ID=your_client_id_here
X_CLIENT_SECRET=your_client_secret_here
X_REDIRECT_URI=http://localhost:8080/callback

# Optional: token file path (default: .token.json)
# X_TOKEN_FILE=.token.json

# === PLAN TIER ===
# Override plan tier: free, basic, pro (can also set in config.yaml)
# X_PLAN=free
